# python-inheritance

This project covers inheritance in Python: superclasses, subclasses,
multiple inheritance, attribute/method overriding, and the built-in
functions `isinstance`, `issubclass`, `type`, and `super`.
